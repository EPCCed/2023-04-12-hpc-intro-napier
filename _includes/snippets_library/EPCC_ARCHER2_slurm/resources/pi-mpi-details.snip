> **Required files:**
> 
> The program used in this example can be retrieved using wget or a browser and copied to the remote.
>
> **Using wget**: {{ site.remote.prompt }} wget {{ site.url }}{{ site.baseurl }}/files/pi-mpi.py
>
> **Using a web browser**:
> <{{site.url}}{{site.baseurl}}/files/pi-mpi.py>
{: .testimonial}
